# TODO

## Архитектурный отложенный вопрос: единый env для монорепо

- Статус: отложено по решению команды, вернуться позже.
- Контекст: обсуждали идею одного корневого `.env` для `apps/api` и `apps/web`.
- Текущая рекомендация: не смешивать backend-секреты и frontend `VITE_*` в одном файле.

### Что проверить при возврате

1. Нужна ли реально единая точка редактирования env или достаточно текущего разделения:
   - `/.env` для API/инфры;
   - `apps/web/.env` для Vite (`VITE_*`).
2. Если хотим частично объединить — рассмотреть компромисс через `/.env.shared` (только несекретные общие значения).
3. Уточнить влияние на:
   - local dev (`npm/turbo`);
   - Docker Compose;
   - CI/CD;
   - onboarding.
4. Принять финальное решение и синхронизировать документы:
   - `docs/14-onboarding-setup.md`;
   - `docs/13-build-stages.md`;
   - `docs/08-deployment.md`;
   - при необходимости `docs/10-monorepo-structure.md`.
